<%= form_for([@client, @computer]) do |f| %>

  <%= error_messages @computer %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
